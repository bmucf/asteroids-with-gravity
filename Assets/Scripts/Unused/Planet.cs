using UnityEngine;

//public class Planet : FloatingBody
//{
//    public GameObject gravPrefab;

//    // Start is called once before the first execution of Update after the MonoBehaviour is created
//    void Start()
//    {
//        mass = Random.Range(1f, 20f);

//        transform.localScale = Vector3.one * mass;

//        GameObject gravField = Instantiate(gravPrefab, transform);
//        gravField.transform.localScale *= Mathf.Sqrt(mass);
//    }
//}
